# awsps  
AWS Profile Switcher  

## Installation  
  
* Clone the repo  
* `cd` to the awsps' directory
* Run `npm run build`
* Run `npm link -g`
  

## Usage  
  
* You can run `awsps --help` for more information.
  